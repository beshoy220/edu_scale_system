import 'package:flutter/material.dart';

class TeacherCompetitionsPage extends StatelessWidget {
  const TeacherCompetitionsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(
        appBar: AppBar(title: Text('Competitions')),
        body: Text('Under Construction'),
      ),
    );
  }
}
